#ifndef DLIB_CC_INCLUDE_LIB_INDEX_H
#define DLIB_CC_INCLUDE_LIB_INDEX_H
/**
 * Include this .h file in your source file if you want to use any other librarys that is outside of dlib_cc directory instead of that include them directoly.
 * That's because that if the path of an other library which is used in dlib_cc library changed, you have to re-write the path of it in all of files that old one is writen.
 */

// cocos2d-x
#include "../../cocos2d/cocos/cocos2d.h"
// dlib
#include "../dlib/dlib.h"

#endif
